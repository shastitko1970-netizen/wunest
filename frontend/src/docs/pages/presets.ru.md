# Пресеты

Пресет — сохранённый набор параметров генерации. Один пресет каждого типа можно «применить» (он используется по умолчанию во всех чатах) или назначить per-chat. Импорт из SillyTavern совместим.

В WuNest пять типов пресетов — каждый отвечает за свой слой запроса. Понимание «что куда влияет» помогает не крутить наугад.

---

## Типы и кто за что отвечает

| Тип | За что отвечает | Когда трогать |
|---|---|---|
| **Sampler** | Стиль и креативность генерации (temperature, top-p, max_tokens, штрафы) | Часто. Главный регулятор «оригинальности vs предсказуемости» |
| **Instruct** | Обёртки турнов в text-completion API (`<|im_start|>`, `[INST]`, и т.д.) | Только для local моделей через completion endpoint. Для chat-completion (OpenAI, Claude) НЕ нужен |
| **Context** | Шаблон сборки story/контекста (`{{description}}`, `{{personality}}`, формат истории) | Редко. Меняй если хочешь свою архитектуру системного промпта |
| **Sysprompt** | Текст системного промпта, заменяющий дефолтный карточки | Часто. Для жанровых направляющих, ролевых правил, jailbreak'ов |
| **Reasoning** | Префиксы/суффиксы для thinking-блоков (`<think>...</think>`) | Только для thinking-моделей (Claude 3.7+, o1, DeepSeek-R1) |

---

## Sampler — главный регулятор

Параметры передаются upstream как есть (через WuApi или BYOK). Как именно их интерпретирует модель — зависит от провайдера. Ниже — нейтральный смысл.

### Температура (`temperature`)

Самый важный параметр. Определяет «случайность» выбора следующего токена.

| Значение | Эффект |
|---|---|
| `0.0` | Детерминированно. Тот же prompt → тот же ответ. |
| `0.3–0.6` | Консервативно. Меньше вариативности, меньше дикости. |
| `0.7–0.9` | Дефолт большинства моделей. Хороший баланс. |
| `1.0–1.3` | Креативно. Больше неожиданностей, чаще «хорошие сюрпризы». |
| `>1.5` | Часто шум. На некоторых моделях — лотерея. |

**Совет:** для длинного ролеплея 0.85–1.0 обычно лучше дефолта 0.7. Модель меньше повторяется.

### Top-P (nucleus sampling)

Отсекает «хвост» маловероятных токенов. `top_p=0.9` оставляет токены, чья суммарная вероятность ≤ 90%.

- `1.0` — без отсечки (самый креативный)
- `0.9` — мягкий фильтр
- `0.7` — жёсткий, текст становится «безопаснее»

Обычно крутят либо `temperature`, либо `top_p`, не оба сразу.

### Top-K

Берёт только N самых вероятных токенов. `top_k=40` — стандарт для местных моделей.

`0` или unset — отключено (не каждый провайдер поддерживает).

### Min-P

Альтернатива top-p, появилась позже. Отсекает токены ниже порога относительной вероятности. `min_p=0.05` — норма.

Часто **лучше** top-p для ролеплея — меньше «обрубает интересные ответвления».

### Max tokens

Жёсткий потолок длины ответа.

| Значение | Когда |
|---|---|
| 200–400 | Короткие реплики, чат-формат |
| 600–1200 | Дефолт. Полноценные сцены. |
| 2000+ | Long-form, narrative scenes |
| Не задано | Модель решает сама (часто 4096) |

### Штрафы (penalties)

Все три штрафа понижают вероятность повторов. Эффект перекрывается, **не комбинируй три сразу**.

- **Frequency penalty** — штрафует токены пропорционально частоте появления. `0.0`–`2.0`. `0.3–0.7` — норма для ролеплея.
- **Presence penalty** — штрафует **за факт появления**, независимо от частоты. `0.0`–`2.0`.
- **Repetition penalty** — мультипликативный штраф (local models, llama.cpp / koboldcpp). `1.0`–`1.3`. `1.0` = выкл.

**Совет:** если модель «зацикливается» на повторах — добавь `frequency 0.5` или `presence 0.3`.

### Seed

Фиксирует random source. Тот же `prompt` + `seed` + `temperature 0` → тот же выход. Полезно для дебага.

`-1` или unset — каждый запрос новый seed.

### Stop strings

Список строк, при появлении которых генерация останавливается. Например, `["{{user}}:", "\nUser:", "\n\n"]` — чтобы модель не доигрывала за `{{user}}`.

В WuNest формат — массив строк (`string[]`) или одиночная строка.

---

## Instruct — для text-completion моделей

В чём разница:

- **Chat-completion** (OpenAI, Claude API, OpenRouter chat) — модель сама знает что такое «role:user» и «role:assistant». Никаких instruct-обёрток не нужно.
- **Text-completion** (local llama.cpp, KoboldCpp, raw OpenRouter completion) — модель видит сырой текст. Instruct preset сообщает «вот так выглядит конец user-турна, вот так — начало assistant-турна».

### Поля

- **input_sequence** — что вставляется перед user-сообщением (например, `<|im_start|>user\n`)
- **output_sequence** — что вставляется перед assistant-сообщением
- **system_sequence** — обёртка system-сообщения
- **stop_sequence** — где модель должна остановиться
- **wrap** — собирать ли всё в одну strings или разделять newline'ами

### Когда трогать

Только если используешь local провайдер через `/completion` endpoint (BYOK с `base_url` указывающим на kobold/llama.cpp/text-generation-webui). Для большинства cloud-провайдеров instruct не применяется.

### Готовые шаблоны

В большинстве импортов ST уже есть `ChatML`, `Llama3 Instruct`, `Mistral Instruct`. Импортируй и применяй — не пиши с нуля.

---

## Context — формат сборки истории

Каркас, по которому WuNest собирает финальный системный промпт.

### Поля

- **story_string** — шаблон с макросами `{{description}}`, `{{personality}}`, `{{scenario}}`, `{{persona}}`, `{{wiBefore}}`, `{{wiAfter}}` и т.д. Это «как мы клеим карточку».
- **chat_start** — разделитель между storyString'ом и историей сообщений
- **example_separator** — разделитель между блоками `mes_example`

### Дефолт

Дефолтный `story_string` примерно такой:

```
{{description}}

{{personality}}

[Scenario: {{scenario}}]

{{wiBefore}}

{{persona}}

{{wiAfter}}
```

Меняй только если делаешь экзотическую архитектуру (например, всё под `[INST]…[/INST]` обёртку для local-моделей).

---

## Sysprompt — текст системного промпта

Заменяет дефолтный «You are {{char}}, a chat companion…». Используется чаще всего из всех типов пресетов.

### Когда нужен

- **Жанровый guidance** — «Это нуар. Атмосфера 1940-х, Лос-Анджелес.»
- **Роль модели** — «You are a Game Master. Your job is to react to {{user}}'s actions, not roleplay {{char}} directly.»
- **Стиль письма** — «Respond in third person, past tense, descriptive prose.»
- **Jailbreak / removal of refusals** для NSFW и других сценариев (на свой страх и риск)

### Структура

Наиболее рабочий шаблон:

```
You are {{char}}. {{char}}'s description, personality and scenario follow:
{{description}}
{{personality}}
{{scenario}}

[Format]
- Reply in third person, past tense.
- Use markdown for actions: *italics for actions, dialogue in quotes*.
- {{char}} is autonomous. {{user}} controls only their own character.

[Don't]
- Don't break the fourth wall.
- Don't speak or act for {{user}}.
```

### Post-history instructions

Отдельное поле в пресете — `post_history_instructions`. Вставляется в конец промпта (после всей истории). Используй для жёстких напоминаний:

```
Stay in character. Do not speak for {{user}}. Reply in third person.
```

Эффективно когда модель «уплывает» к концу длинного чата.

---

## Reasoning — для thinking-моделей

Современные thinking-модели (Claude 3.7+, o1, DeepSeek-R1) генерируют скрытый блок размышлений перед ответом. Пресет описывает как этот блок отделять.

### Поля

- **prefix** — маркер начала thinking-блока (например, `<think>`)
- **suffix** — маркер конца (`</think>`)
- **separator** — что между thinking и финальным ответом

WuNest вырезает thinking-блок из видимого ответа, оставляя только финальную часть. Сам thinking сохраняется в metadata сообщения, доступен через **долгое нажатие на сообщение → «Показать рассуждения»**.

### Когда трогать

Только если используешь thinking-модель и WuNest «не понимает» где заканчиваются размышления (видишь `<think>` в самом тексте ответа). Дефолт работает для Claude и o1.

---

## Управление пресетами

**Library → Presets** — список, сгруппированный по типу. Для каждого пресета:

- **Карандаш** — открыть в редакторе формы
- **Code-braces** — показать raw JSON
- **Download** — экспорт в ST-совместимый JSON
- **Звезда** — сделать «применённым» для этого типа (используется во всех чатах где нет per-chat override)
- **Корзина** — удалить

### Per-chat override

В drawer'е чата (`mdi-tune-variant` в шапке) можно выбрать пресет именно для этого чата. Сохраняется в `chat_metadata.sampler.preset_id`. Перезаписывает «применённый» дефолт.

### Импорт

**Presets → Import** принимает JSON. Тип определяется автоматически по signature-ключам:

- `input_sequence`, `output_sequence`, `wrap` → instruct
- `story_string`, `chat_start`, `example_separator` → context
- `temperature`, `top_p`, `max_tokens`, `frequency_penalty` → sampler
- `prefix`, `suffix`, `separator` (без sequence ключей) → reasoning
- `openai_model`, `temp_openai` → openai legacy

Если детектор не уверен — показывается dropdown для ручного выбора.

### Soft validation

При сохранении сервер проверяет что `data` — JSON-объект (не null/массив/скаляр) и что базовые типы соответствуют (числа — числа, строки — строки, `stop` — `string[]` или single string). Неизвестные поля пропускаются без ошибок — ST-расширения round-trip'ятся.

---

## Связанное

- [`byok`](byok) — свои API-ключи и кастомные base_url для local моделей
- [`memory`](memory) — Author's Note и summary, действуют поверх любого пресета
- [`characters`](characters) — `system_prompt` поле карточки имеет приоритет над пресетом sysprompt
